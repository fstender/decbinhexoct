{
  "name": "decbinhexoct",
  "version": "0.0.1",
  "description": "Simple converter between decimal, binary, hex, and octal numbers",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fstender/decbinhexoct.git"
  },
  "author": {
    "name": "Frank Stender",
    "email": "me@fstender.de"
  },
  "dependencies": {
    "gatsby": "^2.4.3",
    "gatsby-plugin-manifest": "^2.1.1",
    "gatsby-plugin-offline": "^2.1.0",
    "gatsby-plugin-react-helmet": "^3.0.12",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-helmet": "^5.2.1"
  },
  "scripts": {
    "develop": "gatsby develop",
    "build": "npm run clean && gatsby build",
    "deploy": "npm run clean && gatsby build --prefix-paths && gh-pages -d public",
    "serve": "gatsby serve",
    "clean": "rimraf .cache public",
    "format": "prettier --write '**/*.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "prettier": "^1.17.0",
    "rimraf": "^2.6.3"
  },
  "keywords": [
    "decimal",
    "binary",
    "hex",
    "octal",
    "converter"
  ],
  "license": "MIT"
}
